{
  "name": "ridehub",
  "private": true,
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=api-gateway\" \"npm run dev --workspace=auth\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
